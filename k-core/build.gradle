group 'me.superkoh.kframework'
version '1.0.0'

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId group
            artifactId 'core'
            version version

            from components.java

            artifact sourceJar {
                classifier "sources"
            }
        }
    }
}